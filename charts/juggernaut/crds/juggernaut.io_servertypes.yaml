---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: servertypes.juggernaut.io
spec:
  group: juggernaut.io
  names:
    kind: ServerType
    listKind: ServerTypeList
    plural: servertypes
    shortNames:
    - jst
    singular: servertype
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.transport
      name: Transport
      type: string
    - jsonPath: .spec.tokenMode
      name: Token
      type: string
    - jsonPath: .status.activePods
      name: Active
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerType is a rendered server definition.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ServerTypeSpec is the fully defaulted rendering of one servers[] entry of
              juggernaut.yaml. ConfigHash identifies the config version it came from.
            properties:
              args:
                items:
                  type: string
                type: array
              command:
                items:
                  type: string
                type: array
              configHash:
                type: string
              description:
                type: string
              egress:
                items:
                  description: EgressRule is one allowed destination.
                  properties:
                    host:
                      type: string
                    ports:
                      items:
                        format: int32
                        type: integer
                      type: array
                    protocol:
                      type: string
                  required:
                  - host
                  - ports
                  - protocol
                  type: object
                type: array
              env:
                additionalProperties:
                  type: string
                type: object
              httpPath:
                type: string
              httpPort:
                format: int32
                type: integer
              idleTimeout:
                type: string
              image:
                type: string
              maxPods:
                format: int32
                type: integer
              maxSessionAge:
                type: string
              readinessPort:
                format: int32
                type: integer
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              runtimeClassName:
                type: string
              security:
                description: SecuritySpec carries per-server security overrides.
                properties:
                  extraWritablePaths:
                    items:
                      type: string
                    type: array
                  readOnlyRootFilesystem:
                    type: boolean
                  runAsGroup:
                    format: int64
                    type: integer
                  runAsUser:
                    format: int64
                    type: integer
                  writableTmp:
                    type: boolean
                required:
                - readOnlyRootFilesystem
                - runAsGroup
                - runAsUser
                type: object
              tokenEnv:
                type: string
              tokenFile:
                type: string
              tokenHeader:
                type: string
              tokenMode:
                type: string
              transport:
                type: string
              wrapperConfigJSON:
                description: WrapperConfigJSON is the rendered wrapper configuration
                  mounted into the pod.
                type: string
              wrapperPort:
                format: int32
                type: integer
            required:
            - configHash
            - idleTimeout
            - image
            - maxPods
            - maxSessionAge
            - readinessPort
            - tokenMode
            - transport
            - wrapperConfigJSON
            - wrapperPort
            type: object
          status:
            description: ServerTypeStatus reports aggregate pod counts.
            properties:
              activePods:
                format: int32
                type: integer
            required:
            - activePods
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
