# Development secrets for kind ONLY. Generate real ones for anything else:
#   kek: head -c 32 /dev/urandom | base64
apiVersion: v1
kind: Secret
metadata:
  name: juggernaut-secrets
  namespace: juggernaut-system
type: Opaque
stringData:
  broker-client-secret: juggernaut-gateway-secret
  kek: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
